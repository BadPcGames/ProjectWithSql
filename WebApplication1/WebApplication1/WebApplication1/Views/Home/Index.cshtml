@{
    ViewData["Title"] = "Home Page";
}
@model IEnumerable<BlogViewModel>

<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        .content {
            border-radius: 5px;
            border:solid 5px;
            padding:5px;
            margin:15px;
        }
    </style>
</head>



<h1>Blogs</h1>

<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-sm-10 col-md-4 ">
                <div class="content">
                    <h2>
                        <a asp-controller="Posts" asp-action="Index" asp-route-id="@item.Id">@item.Name</a>
                    </h2>
                    <p>
                        @item.Description
                    </p>
                    <p>
                        @item.AuthorName
                    </p>
                </div>
            </div>
          
        }
    </div>
</div>


